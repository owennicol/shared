{
  "name": "@learningtogether/shared",
  "version": "1.0.8",
  "description": "Shared package for learning together code",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "prepare": "npm run clean && npm run build",
    "test": "node --test --loader ts-node/esm src/**/*.test.ts"
  },
  "author": "Owen  Nicol",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "https://github.com/owennicol/shared.git"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
